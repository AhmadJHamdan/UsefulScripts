Just Download this script and place it in the directory that contains IPs.

Name the file containing the IPs (with each IP on a line) IPs.txt
Start Powershell with the user authorized to read logs
Navigate to the directory and run the script from powershell

If any error occurs in the procedure you'll find the ip that caused the error and the root cause of this error.
